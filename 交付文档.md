# 阿里巴巴国际站商品图片/视频抓取系统 · 交付文档

> 版本：v1.0 ｜ 状态：方案定稿（待实施） ｜ 撰写日期：2026-08-12 ｜ 项目代号：getImages

---

## 1. 项目概述

### 1.1 项目背景

电商运营与选品分析过程中，需要批量获取商品的图片与视频素材（主图、详情图、主图视频、详情视频），用于竞品研究、素材归档与内部参考。本项目面向**阿里巴巴国际站（Alibaba.com）**，建设一个可批量、可管理、可扩展的图片/视频抓取系统。

### 1.2 项目目标

- 输入一批商品详情页 URL，自动抓取每件商品的**主图、详情图、主图视频、详情视频**四类媒体资源；
- 以 Web 应用形态交付：浏览器中上传 URL 列表、创建/管理抓取任务、实时查看进度与日志、打包下载结果；
- 以**全栈工程**标准建设：前后端分离、SQLite 持久化、异步任务调度、可量化验收；
- 架构预留**多平台扩展**能力（速卖通 / 1688 / 京东等），现阶段仅实现阿里巴巴国际站。

### 1.3 交付范围

| 项目 | 范围 |
| --- | --- |
| 本期范围 | 阿里巴巴国际站商品详情页的四类媒体资源抓取 |
| 数据源 | 仅抓取**公开可见**的页面与媒体资源，不涉及登录态 |
| 输入 | 商品详情页 URL 列表（txt/csv） |
| 输出 | 按商品组织的媒体目录 + JSON 清单 + 打包下载（zip） |
| 交付形态 | 全栈 Web 应用（FastAPI 后端 + Vue 3 前端） |
| 明确不做 | 抓取非公开数据、破解验证码、绕过付费墙、自动下单/比价等 |

### 1.4 名词定义

| 术语 | 定义 |
| --- | --- |
| 主图 | 商品头图/相册图（gallery），详情页顶部轮播展示的图片集合 |
| 详情图 | 商品详情描述（description）HTML 中嵌入的图片 |
| 主图视频 | 商品主图区域的展示视频（多为 mp4，URL 内嵌于页面数据） |
| 详情视频 | 详情描述区域中的视频（`<video>` 或视频链接） |
| 商品 ID | 阿里国际站商品唯一标识（数字，详情页 URL 末段） |

---

## 2. 需求规格

### 2.1 功能需求

#### 2.1.1 数据抓取（核心）

- **输入解析**：读取 URL 列表文件（txt 每行一个 URL / csv 含 `url` 列），提取商品 ID 并去重、校验合法性。
- **媒体提取**：每件商品抓取四类资源——
  - 主图：`window.detailData.product.mediaItems` 中 `type == "image"` 项，URL 取 `imageUrl.big` 优先、`normal` 兜底；
  - 主图视频：同数据源中 `type == "video"` 项的视频 URL（mp4，内嵌页面数据，无需触发播放）；
  - 详情图：详情描述 HTML（`productHtmlDescription`）中所有 `<img>` 链接；
  - 详情视频：详情描述 HTML 中的 `<video>`/mp4 链接（若懒加载需滚动触发）。
- **下载落盘**：按目录规则保存，图片统一转 jpg（兼容 avif/webp 源），视频保留 mp4 原格式。
- **结果清单**：每件商品生成 `manifest.json`，含商品 ID、标题、四类资源清单、抓取时间与状态。
- **失败处理**：单件失败不中断整体任务，生成失败报告并支持重试。

#### 2.1.2 全栈应用（管理端）

- **任务管理**：创建任务（上传 URL 列表、设置并发/限速参数）、查看任务列表、启动/暂停/取消任务。
- **实时进度**：任务级（完成商品数/总数、成功率）与商品级（四类资源完成状态）进度展示。
- **日志流**：实时滚动查看抓取日志（按商品、按错误级别过滤）。
- **结果下载**：任务完成后打包下载全部媒体（zip），或按商品单独下载。
- **断点续传**：任务中断/取消后可续跑，已下载资源跳过不重复下载。
- **历史记录**：任务历史与结果留存于 SQLite，可回溯。

### 2.2 非功能需求

| 类别 | 要求 |
| --- | --- |
| 限速合规 | 全局请求限速（默认 1–5 秒/请求），尊重 `Retry-After`，避免对目标站造成压力 |
| 命名规范 | 见 3.4 节目录与命名规则，稳定可读、跨平台兼容 |
| 断点续传 | 资源级幂等：已存在且校验通过的文件跳过 |
| 可用性 | 单机部署即可运行；任务失败可重试，不丢失已抓取成果 |
| 可维护性 | 前后端分离、模块化、配置集中、日志分级 |
| 合规声明 | 见 3.10 节：仅公开数据、遵守 ToS/robots、尊重版权、商业用途需授权 |

---

## 3. 技术方案

### 3.1 技术选型

| 层 | 选型 | 版本要求 | 理由 |
| --- | --- | --- | --- |
| 语言 | Python | **建议 3.10+** | 生态成熟；注：最新版 FastAPI/Pydantic/Playwright 均要求 ≥3.9/3.10，见 3.9 |
| 后端框架 | FastAPI | ≥3.10（最新版） | 异步 REST、自动文档、类型校验，适合 Web 全栈 |
| HTTP 客户端 | httpx + curl_cffi | ≥3.8 / ≥3.9 | httpx 做常规请求；curl_cffi 模拟浏览器 TLS 指纹（反爬 L2） |
| HTML/JSON 解析 | lxml + BeautifulSoup4 | — | 解析 `window.detailData` JSON 与详情描述 HTML |
| 浏览器兜底 | Playwright + playwright-stealth | ≥3.10（最新版） | 动态渲染与反爬 L3 兜底，确定性最强 |
| 存储 | SQLite（stdlib 内置） | 内置 | 单机足够，任务/商品/资源状态持久化，零运维 |
| 前端 | Vue 3 + Element Plus + Vite | Node 16+（本机 22 ✓） | Element Plus 组件丰富、中文文档友好 |
| 打包下载 | zipfile（stdlib） | 内置 | 任务结果打包 |

### 3.2 系统架构

```text
┌─────────────┐   REST/SSE    ┌──────────────────┐   调度   ┌──────────────────┐
│ 前端页面      │ ───────────▶ │  FastAPI 后端      │ ──────▶ │ 任务调度器         │
│ Vue3+Element │ ◀─────────── │  (REST API + 鉴权) │ ◀────── │  (asyncio 后台任务)│
│ Plus         │  进度/日志流  │                   │         └────────┬─────────┘
└─────────────┘              └──────────────────┘                   ▼
                                                          ┌──────────────────┐
                                                          │ 抓取引擎          │
                                                          │  fetcher(三层递进)│
                                                          │  extractor(提取)  │
                                                          │  downloader(下载) │
                                                          └────────┬─────────┘
                                                     ┌──────────────┴─────────────┐
                                                     ▼                            ▼
                                              ┌─────────────┐            ┌────────────────┐
                                              │ SQLite      │            │ data/downloads/ │
                                              │ 任务/商品/资源│            │ 媒体文件+清单    │
                                              └─────────────┘            └────────────────┘
```

数据流：前端上传 URL 列表 → 创建任务 → 调度器逐商品执行「获取详情页 → 提取媒体 URL → 下载落盘 → 更新状态」→ 任务完成后可打包下载。

### 3.3 数据模型（SQLite）

| 表 | 关键字段 | 说明 |
| --- | --- | --- |
| `tasks` | id, name, url_file, status(pending/running/paused/cancelled/done/partial), total, succeeded, failed, rate_limit, concurrency, created_at, started_at, finished_at | 任务主表 |
| `products` | id, task_id(FK), product_id, url, title, status(pending/fetching/done/failed), error, fetched_at | 商品维度 |
| `resources` | id, product_id(FK), kind(main_image/detail_image/main_video/detail_video), url, file_path, size, status(pending/downloading/done/failed), retries | 资源维度，支撑断点续传 |

进度与成功率由聚合查询得出；**日志不落库**（写文件日志，避免膨胀）。

### 3.4 项目目录结构与命名规范

```text
getImages/
├─ backend/
│  ├─ app/
│  │  ├─ main.py          # FastAPI 入口（挂路由 + 静态托管前端产物）
│  │  ├─ config.py        # 限速、并发、UA 池、超时配置
│  │  ├─ db.py            # SQLite 连接与建表
│  │  ├─ models.py        # 数据访问层
│  │  ├─ api/             # tasks.py / products.py / download.py
│  │  ├─ scheduler.py     # 异步任务调度器
│  │  └─ engine/
│  │     ├─ fetcher.py    # 三层递进获取（httpx → curl_cffi → Playwright）
│  │     ├─ extractor.py  # detailData/描述 HTML 媒体提取
│  │     ├─ downloader.py # 下载、格式转换、断点续传、校验
│  │     └─ anti_anti.py  # UA 轮换、退避重试、stealth 配置
│  ├─ requirements.txt
│  └─ tests/
├─ frontend/
│  ├─ src/
│  │  ├─ views/           # TaskListView / TaskDetailView / ProductView
│  │  ├─ components/      # ProgressPanel / LogStream / MediaPreview
│  │  ├─ api/             # axios 封装
│  │  └─ App.vue / main.js
│  └─ package.json / vite.config.js
├─ data/                  # 运行时数据（gitignore）
│  ├─ downloads/{task_id}/{product_id}/…
│  └─ app.db
└─ 交付文档.md
```

媒体落盘命名规则（`data/downloads/{task_id}/{product_id}/`）：

| 资源 | 命名 | 说明 |
| --- | --- | --- |
| 主图 | `main_001.jpg` | 序号从 001 递增 |
| 详情图 | `detail_001.jpg` | 同上 |
| 主图视频 | `main_video_01.mp4` | 序号从 01 递增 |
| 详情视频 | `detail_video_01.mp4` | 同上 |
| 清单 | `manifest.json` | 商品 ID/标题/四类资源路径/状态/抓取时间 |

### 3.5 API 设计草案

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/tasks` | multipart 上传 URL 文件 + 参数（限速/并发），创建任务 |
| GET | `/api/tasks` | 任务列表（分页） |
| GET | `/api/tasks/{id}` | 任务详情 + 进度聚合 |
| POST | `/api/tasks/{id}/start` `/pause` `/cancel` | 任务控制 |
| GET | `/api/tasks/{id}/products` | 商品列表（含资源状态） |
| GET | `/api/tasks/{id}/logs` | 日志流（SSE 实时推送） |
| GET | `/api/tasks/{id}/download` | 打包下载 zip |
| GET | `/api/products/{id}/resources` | 单商品四类资源明细 |

### 3.6 前端设计（Element Plus）

| 页面 | 核心组件 |
| --- | --- |
| 任务列表 | `el-upload`（拖拽上传）、`el-table` + 状态 `el-tag`、操作按钮 |
| 任务详情 | `el-progress` 进度、`el-table` 商品维度、`el-scrollbar` + `el-timeline` 日志流（SSE） |
| 商品详情 | `el-tabs` 四类资源、`el-image` 预览、`video` 标签播放 |
| 结果下载 | `el-button` 打包下载、失败重试入口 |

### 3.7 抓取引擎详细方案

**获取（三层递进）**：目标站采用阿里云 WAF + Bot Management（TLS/JS 指纹、headless 信号、IP 信誉拦截）。

1. **L1 轻量请求**：httpx + 完整浏览器请求头（Accept / Accept-Language / sec-ch-ua / Referer）+ 会话保持；适合未触发风控的访问；
2. **L2 TLS 指纹模拟**：L1 返回 403/连接被重置时降级 curl_cffi（模拟 Chrome TLS/JA4 指纹）；
3. **L3 浏览器兜底**：仍失败则 Playwright + stealth（覆盖 `navigator.webdriver`、真实 UA、禁用 `AutomationControlled`），确定性兜底。

**重试与限速**：429/5xx 按 `2^n` 指数退避重试（默认最多 3 次），尊重 `Retry-After`；**403 不盲目重试**，视为需要切换抓取层级；全局令牌桶限速（默认 1–5 秒/请求，可配），并发默认 1–3。

**媒体提取**：

| 资源 | 提取路径 |
| --- | --- |
| 主图 | 正则 `window\.detailData\s*=\s*(\{)` 提取 JSON → `product.mediaItems` 中 `type=="image"`，URL 取 `imageUrl.big` 优先、`normal` 兜底 |
| 主图视频 | 同上数据源中 `type=="video"` 项的 mp4 URL（内嵌页面数据，无需触发播放） |
| 详情图 | `productHtmlDescription`（描述 HTML）中全部 `<img>` 链接（lxml 解析） |
| 详情视频 | 描述 HTML 中 `<video>`/mp4 链接；若懒加载则滚动触发后重新提取 |

**下载与校验**：httpx 流式下载、超时 30s；0 字节/大小不符视为失败并重试；图片 avif/webp 源用 Pillow 统一转 jpg（avif 需 `pillow-avif-plugin`，转换失败则保留原格式）；视频保留 mp4 原格式，可选 ffprobe 完整性校验；已存在且大小匹配的文件跳过（断点续传）。

### 3.8 备选方案对比

| 方案 | 优点 | 缺点 | 结论 |
| --- | --- | --- | --- |
| 官方开放平台 API（`alibaba.product.get` 类） | 合规、稳定、字段规范 | 需企业账号 + OAuth2 签名 + 原始数据包保密协议，门槛高 | 备选（有条件时优先） |
| 商业 scraping API（ScrapeOps/BrightData/Oxylabs/scrape.do/piloterr/Apify actor） | 免反爬维护、稳定 | 按量付费、字段受服务商限制 | 备选/演进 |
| **自建三层抓取（本期方案）** | 可控、零订阅成本、字段自定 | 需随反爬升级维护 | **主方案** |

### 3.9 部署与运行

- **环境要求**：Python **3.10+**（当前开发机 3.8.6 不满足最新版 fastapi 0.141/uvicorn 0.52/playwright 1.62 的 `>=3.10` 要求，需升级；锁定旧版 fastapi 0.115.x 等不推荐）、Node 16+（本机 22 ✓）、SQLite 内置。
- **后端**：`pip install -r backend/requirements.txt` → `uvicorn app.main:app --port 8000`，首次启动自动建表。
- **前端**：`npm install` → `npm run dev`（Vite，`/api` 代理到 `127.0.0.1:8000`）；生产构建产物由 FastAPI 静态托管，单端口部署。
- **数据目录**：`data/` 自动创建，`app.db` 与下载文件均在其中。

### 3.10 合规与安全

- 仅抓取**公开可见**页面与媒体，不登录、不破解验证码、不绕过付费/登录墙；
- 遵守目标站 `robots.txt` 与服务条款，限速访问、避免对目标站造成负担；
- 素材版权归原权利人，本系统产出仅限学习与研究等合法用途，商业使用需获得授权或改用官方 API；
- 对个人/企业数据抓取的法律边界（如目标地域的数据保护法规）由使用者自行评估并承担相应责任。

---

## 4. 实施计划与验收

### 4.1 里程碑

| 里程碑 | 内容 | 交付物 | 验证方式 |
| --- | --- | --- | --- |
| M1 抓取原型 | 脚本级实现：单商品验证四类资源可提取、可下载 | 原型脚本 + 1 件商品样本 | 四类资源落盘成功 |
| M2 后端 API + 任务系统 | FastAPI + SQLite + 调度器 + 三层递进 fetch | 后端服务、任务/商品/资源三表 | 接口测试 + 任务全流程跑通 |
| M3 前端界面 | Vue 3 + Element Plus：任务/进度/日志/下载 | 前端页面 | 端到端人工验收 |
| M4 稳健化与联调 | 限速/重试/断点续传/格式转换/前后端联调 | 完整应用 | 4.3 验收标准逐项核对 |
| M5 交付验收 | 全量验收 + 部署说明 + 文档定稿 | 交付包 | 验收清单通过 |

### 4.2 测试策略

- **单元测试**（pytest）：URL/商品 ID 提取正则、`detailData` 解析（样例数据 mock）、命名与目录规则、指数退避重试、断点续传幂等、格式转换降级；
- **冒烟测试**：取 3–5 个真实商品 URL 端到端跑通四类资源抓取与清单生成（低速、限量）；
- **前端测试**（可选）：关键页面组件渲染冒烟；
- **回归**：反爬层级切换、任务中断续跑等场景加入回归用例。

### 4.3 验收标准

| 编号 | 验收项 | 量化标准 |
| --- | --- | --- |
| A1 | 四类资源提取正确率 | 抽样人工核对 ≥ 95% |
| A2 | 图片下载成功率 | ≥ 98%（排除目标站主动拦截） |
| A3 | 任务管理全流程 | 创建/启动/暂停/取消/续跑均可用 |
| A4 | 进度与日志实时性 | SSE 推送，状态与日志可见即更新 |
| A5 | 打包下载 | zip 内容与目录命名规则一致 |
| A6 | 清单完整性 | manifest.json 字段齐全（ID/标题/四类资源/状态/时间） |
| A7 | 限速生效 | 实测请求间隔符合配置值 |
| A8 | 断点续传 | 中断后续跑不重复下载已完成资源 |

### 4.4 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 反爬策略升级 | 抓取失败率上升 | 三层递进降级 + 可选代理 + 升级 stealth；最坏情况转商业 API |
| 页面结构改版 | 提取失效 | 提取器集中封装，字段变化快速定位修复 |
| 视频懒加载/防盗链 | 详情视频缺失 | 滚动触发重新提取；下载带页面 Referer |
| Python 3.8 不兼容新依赖 | 后端无法启动 | 环境要求明确升级 3.10+（见 3.9） |
| 前端联调返工 | 周期延长 | 接口契约先行（FastAPI OpenAPI 生成前端类型） |
| 图片格式转换失败 | 输出格式不一致 | Pillow 转换失败保留原格式并记录 |

### 4.5 扩展路线

1. **多平台抽象**：定义 `PlatformAdapter` 接口（`fetch_detail` / `extract_media`），新增速卖通/1688/京东仅需实现适配器；
2. **官方 API 接入**：具备企业账号条件时接入开放平台接口，替换自建抓取；
3. **分布式任务**：批量场景引入 Celery + Redis；
4. **登录态支持**：按需增加会话保持（注意合规边界）。

---

## 5. 附录：调研参考

- Alibaba 商品详情页结构：Apify `xtracto/alibaba-product-scraper`、`scraper-engine/alibaba-scraper` 文档；`meta[name="aplus-exdata"]` 含 productId/companyId（StackOverflow）
- `window.detailData` 解析与 `product.mediaItems` 结构：scrape.do《How to Scrape Alibaba Product Listings》；piloterr Alibaba Product API 文档
- 详情描述 HTML（`productHtmlDescription`）：BitBash Product 字段说明
- 反爬与应对：Scraperly《How to Scrape Alibaba (Hard)》；dev.to《Overcoming Alibaba's Anti-Scraping Stack》；ScrapeOps/Rebrowser 重试与 403 处理实践；scraper-bank/Alibaba.com-Scrapers
- 同类工具（需求对齐）：当图、BabaScraper、AliExpress & Alibaba Video Downloader、搞图宝、AliStore
- 版本兼容性：PyPI / npm registry 元数据实测（2026-08）

---
