# getImages · 实施验收记录

> 实施方式：多子智能体并行（引擎 / 后端 API+任务系统 / 前端三路并行，按 `CONTRACT.md` 对齐）
> 验收日期：2026-08-12 ｜ 对应 PRD §7 与《交付文档.md》§4.3

## 1. 交付形态

全栈 Web 应用，单端口部署：

- **后端**：FastAPI + SQLite（`data/app.db`）+ asyncio 任务调度器 + 三层递进抓取引擎（httpx → curl_cffi → Playwright）
- **前端**：Vue 3 + Element Plus + Vite，构建产物由 FastAPI 静态托管
- **测试**：pytest 71 用例（引擎 48 + API 23），全部通过

## 2. 运行方式

```bash
# 后端（Python 3.10+，本机实测 3.13）
cd backend
pip install -r requirements.txt            # 或使用已就绪的 .venv
python -m uvicorn app.main:app --port 8000 # 首次启动自动建表；前端构建产物存在时自动托管

# 前端开发模式（可选，端口 5173，/api 代理到 8000）
cd frontend
npm install --registry=https://registry.npmmirror.com
npm run dev
```

生产单端口访问 `http://127.0.0.1:8000/` 即可（前端 dist 已由后端托管）。

## 3. 验收核对（A1–A8）

| 编号 | 验收项 | 量化标准 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| A1 | 四类资源提取正确率 | 抽样核对 ≥95% | ⚠️ 部分 | 提取器实现 + 样例 HTML 单元测试（8 资源全量提取、big/normal 优先级）；**真实站点抽样未执行**（目标站 WAF 对无 cookie 首访统一返回 JS 跳转页，L1/L2 均受限） |
| A2 | 图片下载成功率 | ≥98%（排除主动拦截） | ⚠️ 部分 | downloader 单元测试（流式下载/0 字节失败/断点续传/格式转换降级）；真实下载同受 A1 网络限制 |
| A3 | 任务管理全流程 | 创建/启动/暂停/取消/续跑均可用 | ✅ 通过 | uvicorn 实测：创建→start→pause→resume→cancel（running 中）→cancelled→resume 续跑；非法转换 409 正确拒绝 |
| A4 | 进度与日志实时性 | SSE 推送 ≤2s | ✅ 通过 | uvicorn 实测 SSE 流式：任务开始/商品进度/任务结束日志实时推送，帧格式 `data: {...}` 正确 |
| A5 | 打包下载 | zip 与目录命名规则一致 | ✅ 通过 | 测试覆盖：全任务 `{task_id}/{product_id}/` 嵌套、单商品 `{product_id}/` 扁平、仅打包成功资源、运行中 409 |
| A6 | 清单完整性 | manifest 字段齐全 | ✅ 通过 | 实测失败商品 manifest：product_id/url/title/fetched_at/status/四类资源/error 齐全落盘 |
| A7 | 限速生效 | 请求间隔符合配置 | ⚠️ 部分 | RateLimiter 单元测试通过；真实目标站请求间隔未实测（同 A1 网络限制） |
| A8 | 断点续传 | 中断后续跑不重复下载 | ✅ 通过 | 下载器文件级幂等测试（已存在且 size>0 → skipped）+ cancelled→resume 全流程实测 |

**判定**：A3/A4/A5/A6/A8 达标；A1/A2/A7 的功能实现与测试就绪，但**真实站点抽样验收**依赖可访问目标站的网络环境（当前环境 L1/L2 均被 WAF 无 cookie 拦截，属"目标站主动拦截"范畴）。按 PRD §7.2，A1/A2 的抽样核对建议在可达网络环境补测；拦截情形已在 crawler 层做内容级识别（如实标记失败，不虚假成功）。

## 4. 实施期发现并修复的问题

1. **单商品 zip 目录重复**（`download.py`）：全任务/单商品打包目录规则混用，单商品变 `{pid}/{pid}/`；改为显式 `nested` 参数。
2. **SSE 测试挂起**（`test_tasks.py`）：httpx ASGITransport 对无限流全缓冲（app 结束才返回 body），TestClient 无法读无限 SSE；拆为生成器层 + 有限流 HTTP 层两层测试。
3. **反爬跳转页虚假成功**（`crawler.py`）：目标站返回 200 跳转页（无 detailData）时原实现记 0 资源成功；新增内容级空检测，标记失败并写入原因。
4. **curl_cffi 参数**：`follow_redirects` → `allow_redirects`（子代理静态审查发现）。

## 5. 已知限制与后续

- **目标站反爬**：无 cookie 首访被 WAF 拦截（L1 httpx 与 L2 curl_cffi 均返回 JS 跳转页，需 cookie 预热/JS 执行）；L3 Playwright 已按 lazy import 实现，浏览器二进制需 `playwright install chromium` 后启用。后续可增强 cookie 预热（先访问首页获取 `cna`/`x5sec`）或配置代理。
- **前端**：Element Plus 全量引入（主 chunk ~1MB，仅体积警告）；未做浏览器端人工交互验收（构建通过 + 代码审查替代）。
- **数据库**：SQLite 单机部署；>500 商品分批已在调度器按序消费（未压测）。
- **后续迭代**：多平台适配层、官方 API 接入、分布式调度（见交付文档 §4.5）。
