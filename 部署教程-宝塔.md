# getImages · 宝塔面板（Linux）部署教程

> 适用对象：在 Linux 云服务器（Ubuntu / Debian / CentOS 等）上，使用**宝塔面板**部署本系统。
> 对应架构：《交付文档.md》§3.9 部署与运行。本文以宝塔 Linux 面板（9.x，2025–2026 版本）为准。

---

## 0. 部署架构（先看这张图）

```text
浏览器 ──▶ 域名:80/443（Nginx，宝塔「网站」）
              │ 反向代理（全站转发）
              ▼
        http://127.0.0.1:8000（FastAPI：REST API + SSE + 静态托管 frontend/dist）
              │
              ├── data/app.db        （SQLite 任务/商品/资源）
              ├── data/downloads/    （媒体文件 + manifest.json）
              └── data/logs/         （任务日志文件）
```

**关键点**：本项目生产模式由 FastAPI 静态托管前端构建产物（`frontend/dist`），
因此**不需要单独部署 Node 站点**——Nginx 把全部请求转发到后端 8000 端口即可。
只有一个站点、一个后端服务进程，运维最简单。

---

## 1. 准备工作

| 项目 | 要求 |
| --- | --- |
| 服务器 | Linux（推荐 Ubuntu 22.04 / Debian 12 / CentOS 7.9+），≥2GB 内存 |
| 域名（可选但推荐） | 一个已备案域名，A 记录解析到服务器公网 IP |
| 端口 | 22（SSH）、8888（宝塔面板）、80/443（网站）——**云厂商安全组 + 系统防火墙都要放行** |

> 后端 8000 端口**不需要对外开放**：Nginx 与后端同机，走 127.0.0.1 回环即可。

---

## 2. 安装宝塔面板

SSH 登录服务器，执行官方安装脚本（脚本若更新，请以宝塔官网 <https://www.bt.cn> 最新为准）：

```bash
# Ubuntu / Debian
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && bash install.sh

# CentOS
yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_6.0.sh && bash install.sh
```

安装完成后执行 `bt default` 查看面板地址、账号、密码（**请立即保存**）。

浏览器打开 `http://服务器IP:8888` 登录面板，建议：

- 修改面板默认端口与账号密码；
- 开启二次验证与登录提醒。

---

## 3. 上传项目代码

在宝塔「文件」中新建目录，例如 `/www/wwwroot/getimages`，然后二选一：

- **方式一（推荐，上传压缩包）**：本地将项目打包（**排除 `.venv/`、`node_modules/`、`data/`、`frontend/dist/`**），上传后右键解压；
- **方式二（Git 拉取）**：在宝塔「终端」中执行

  ```bash
  cd /www/wwwroot
  git clone <你的仓库地址> getimages
  ```

部署后目录结构（以 `/www/wwwroot/getimages` 为例）：

```text
getimages/
├─ backend/
│  ├─ app/                # FastAPI 应用（main.py 入口）
│  ├─ requirements.txt
│  └─ tests/
├─ frontend/              # Vue3 源码（生产构建产物在 dist/）
├─ README.md
└─ …
```

---

## 4. 后端部署（宝塔「Python 项目管理器」）

### 4.1 准备 Python 环境（先创建虚拟环境）

新版宝塔面板已内置「Python 项目管理器」功能（无需单独安装插件），入口为
**「网站 → Python 项目」**。

> **关键前置**：添加 Python 项目时**不能直接选系统级 Python 环境**（会导致创建的项目错误）。
> 正确流程是：先装目标 Python 版本 → **创建虚拟环境** → 建项目时选择该**虚拟环境**。

**步骤 ① 安装 Python 版本（若缺少目标版本）**

1. 「网站 → Python 项目」→ 点击 **「Python环境管理」** 按钮，弹出「Python环境管理」弹窗；
2. 点击弹窗内 **「版本管理」**，打开「Python版本管理」弹窗（标签页：常用版本 / 所有版本）；
3. 若目标版本未部署（安装状态为未安装），点击该版本「操作」列的 **【安装】** 按钮；
   （版本列表为空时可先点 **「更新版本列表」**）
4. 安装 **Python 3.10+**（本项目要求 3.10+，推荐 3.11/3.12/3.13）。

**步骤 ② 创建虚拟环境**

1. 回到「Python环境管理」弹窗，点击 **「创建虚拟环境」**；
2. 填写表单：
   - **名称**（必填）：如 `getimages-env`（**必须英文**）；
   - **环境来源**（单选）：在 **系统级环境 / 宝塔面板环境** 中选择上一步安装的 3.10+ 版本
     （每个选项会展示名称、类型、Python 版本）；
   - **备注**（非必填）：如 `getimages 后端环境`；
3. 点击 **【创建】**。

**步骤 ③ 确认虚拟环境就绪**

环境列表中（字段：名称 / Python 版本 / 解释器位置 / 使用项目 / 备注 / 操作）应出现刚创建的
虚拟环境（列表会区分**系统级环境 / 虚拟环境 / 宝塔面板环境**三种类型）。多余的旧环境可点
「操作 → 删除」清理。

### 4.2 添加 Python 项目

打开 **「网站 → Python 项目」**，点击 **「添加项目」**，按弹窗表单逐项填写：

| 表单字段 | 填写内容 |
| --- | --- |
| 项目名称 | `getimages`（**必须英文**，面板按名称生成项目） |
| Python 环境 | 选择 §4.1 **步骤 ② 创建的虚拟环境**（如 `getimages-env`）；**不可选系统级环境**，否则项目创建错误 |
| 启动方式 | **命令行启动**（本项目安全默认，见下方说明） |
| 项目路径 | `/www/wwwroot/getimages/backend`（含 `app/` 的目录） |
| 启动命令 | `uvicorn app.main:app --host 0.0.0.0 --port 8000` |
| 环境变量 | **无**（如需代理等可选手动配置，见 §4.3 备注） |
| 启动用户 | `www`（默认即可） |
| 安装依赖包 | `/www/wwwroot/getimages/backend/requirements.txt`（非必填，也可安装后补，见 §4.3） |

> 「更多设置」可展开勾选：**开机启动 / 守护进程**（推荐勾选，面板自动拉起服务）。

**启动命令说明**（二选一，均在 backend 目录下执行 `app.main:app`）：

```bash
# 方式 A：uvicorn 单进程（本项目任务调度为单进程 asyncio，够用，推荐）
uvicorn app.main:app --host 0.0.0.0 --port 8000

# 方式 B：gunicorn 多进程（FastAPI 是 ASGI，必须用 UvicornWorker，勿直接 gunicorn app.main:app）
gunicorn app.main:app --workers 2 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --timeout 120
```

> 说明：
>
> - 项目入口是 `backend/app/main.py` 里的 `app` 对象，所以命令是 `app.main:app`，**必须在 backend 目录下运行**（项目路径填 backend）；
> - `--host 0.0.0.0` 必填，否则无法从外部（Nginx 或公网）访问；
> - 内存 ≤2GB 时 gunicorn workers 建议 1–2，避免 OOM；
> - 本项目调度器为单实例 asyncio（SQLite 单机），**勿多开 worker 跑同一 data 目录**，单进程 uvicorn 是安全默认。

**若在表单中选择「gunicorn」启动方式**，对应字段为：

| 字段 | 填写内容 |
| --- | --- |
| 项目路径 | `/www/wwwroot/getimages/backend` |
| 入口文件 | `main:app`（即 `app/main.py` 中的 `app` 对象） |
| 通讯协议 | **asgi**（FastAPI 是异步框架，必须选 asgi；表单提示：Flask 用 wsgi、FastAPI 用 asgi） |
| 项目端口 | `8000` |
| 环境变量 / 启动用户 / 安装依赖包 | 同上方命令行方式 |

> 底部表单提示已说明：**通讯协议** wsgi 为同步（一次处理一个请求），asgi 为异步（并发处理）；FastAPI 必须选 **asgi**。

### 4.3 安装依赖与启动验证

安装依赖两种方式任选（推荐方式一）：

1. **方式一（推荐，创建时自动装）**：在 4.2 添加项目时，「安装依赖包」填入
   `/www/wwwroot/getimages/backend/requirements.txt`，面板会在创建虚拟环境后自动安装；
2. **方式二（补装）**：项目列表 → 该项目「环境管理」→「从 requirements.txt 安装」→ 选择 `backend/requirements.txt`。

国内服务器建议先切换国内 PyPI 源（环境管理或宝塔「终端」中执行）：

```bash
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

安装完成后点击「重启」，查看「项目日志」出现 `Uvicorn running on http://0.0.0.0:8000` 即启动成功。

> **需要登录才能抓取的阿里巴巴商品**：在宝塔「终端」中执行
> `cd /www/wwwroot/getimages/backend && python -m app.engine.login`
> （首次需 `pip install playwright && playwright install --with-deps chromium`，`--with-deps` 会补装 Linux 系统库）。
> 若服务器在国外、商品页无需登录，则跳过此步。
>
> **环境变量用途**：若需通过代理访问（如 Google 登录），可在项目的「环境变量」中指定
> `PLAYWRIGHT_PROXY=http://127.0.0.1:7890`（按你的代理工具端口填写）；httpx/curl_cffi 抓取层自动读取系统代理环境变量。

### 4.4 目录权限（重要）

本项目运行时会在项目根目录自动创建 `data/`（SQLite + 下载文件），
**启动用户 `www` 必须对项目根目录可写**：

```bash
chown -R www:www /www/wwwroot/getimages
chmod -R u+rwX /www/wwwroot/getimages
```

---

## 5. 前端构建

前端源码不需要常驻 Node 服务——只需**构建一次**生成 `frontend/dist`，
由 FastAPI 静态托管。二选一：

- **方式 A（本地构建）**：在有 Node 的机器上执行

  ```bash
  cd frontend
  npm install --registry=https://registry.npmmirror.com
  npm run build
  ```

  将生成的 `frontend/dist/` 上传到服务器 `/www/wwwroot/getimages/frontend/dist/`；

- **方式 B（服务器构建，需宝塔「Node.js 版本管理器」安装 Node 16+）**：宝塔终端执行

  ```bash
  cd /www/wwwroot/getimages/frontend
  npm install --registry=https://registry.npmmirror.com
  npm run build
  ```

构建完成后**重启后端项目**（FastAPI 启动时检测 `frontend/dist/index.html` 存在则自动托管）。

> 本地开发/联调不受影响：开发时 `npm run dev` 走 Vite 的 `/api` 代理到 8000。

---

## 6. Nginx 站点与反向代理

### 6.1 添加站点

宝塔「网站 → 添加站点」：

- 域名：`your-domain.com`（无域名可留空，用 IP 访问）；
- 网站目录：任意空目录（纯反代，无需静态文件）；
- 运行环境：纯静态 / Nginx。

### 6.2 配置反向代理（全站转发）

进入站点「设置 → 反向代理 → 添加反向代理」：

- 代理名称：`getimages-backend`
- 目标 URL：`http://127.0.0.1:8000`
- 发送域名：`$host`（默认）

**然后必须编辑「配置文件」，在 `server {}` 内确认/补齐以下内容**
（宝塔「反向代理」生成的规则可能缺少 SSE 所需配置）：

```nginx
location / {
    proxy_pass http://127.0.0.1:8000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Connection "";

    # 关键：SSE 日志流（/api/tasks/{id}/logs）必须关闭缓冲，否则日志不实时推送
    proxy_buffering off;
    proxy_cache off;

    # 任务打包下载（zip）可能耗时，放宽超时
    proxy_connect_timeout 60s;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;

    # 上传 URL 列表文件
    client_max_body_size 50m;
}
```

保存后点击「重载配置」（不是仅保存）。

> **为什么必须 `proxy_buffering off`**：本项目进度/日志用 SSE（`text/event-stream`）
> 实时推送。Nginx 默认开启响应缓冲，会把 SSE 数据攒到缓冲满才转发，
> 导致前端日志不实时、进度延迟超过 PRD 要求的 ≤2s。关闭后数据即时透传。

---

## 7. SSL / HTTPS（可选但推荐）

1. 站点「设置 → SSL」；
2. 选择 **Let's Encrypt**，勾选域名并申请（需域名已解析到本机、80 端口可访问）；
3. 证书签发后勾选「**强制 HTTPS**」；
4. 宝塔默认自动续期（90 天），无需手工操作。

---

## 8. 验证清单

| 步骤 | 验证方式 | 预期 |
| --- | --- | --- |
| 后端健康 | 宝塔终端 `curl http://127.0.0.1:8000/api/health` | `{"status":"ok"}` |
| 后端文档 | 浏览器 `http://127.0.0.1:8000/docs`（或域名反代后 `/docs`） | Swagger 页面 |
| 前端页面 | 浏览器访问 `https://your-domain.com/` | 任务列表页正常加载 |
| 创建任务 | 页面上传一个 txt（含若干商品 URL），提交 | 任务进入运行中，进度/日志实时滚动 |
| 实时日志 | 任务运行时观察底部日志流 | 日志**逐条实时**出现（验证 SSE 生效） |
| 打包下载 | 任务完成后点击「打包下载」 | 下载 zip，内含 `{task_id}/{product_id}/` 目录与 manifest.json |
| 数据库落盘 | `ls /www/wwwroot/getimages/data/` | 存在 `app.db`、`downloads/`、`logs/` |

---

## 9. 常见问题排查

| 现象 | 原因与处理 |
| --- | --- |
| 502 Bad Gateway | 后端未启动或端口不对：看宝塔「Python 项目」日志；确认启动命令为 `app.main:app`（在 backend 目录）且 `--host 0.0.0.0` |
| ModuleNotFoundError | 依赖未装全：项目「环境管理」→ 从 requirements.txt 安装（或添加项目时在「安装依赖包」填路径）；国内服务器换清华源 |
| Python 环境下拉框无可用版本 / 项目创建错误 | 未创建虚拟环境或选了系统环境：到「网站 → Python 项目 → Python环境管理 → 版本管理」安装 3.10+ → 「创建虚拟环境」生成隔离环境 → 建项目时选择该**虚拟环境**（不可选系统级环境） |
| 前端白屏 / 404 | `frontend/dist` 未上传或未重启后端；确认 `dist/index.html` 存在 |
| 日志不实时、攒一批才出现 | Nginx 未关缓冲：确认 `proxy_buffering off;` 并**重载配置** |
| 打包下载慢/超时 | 放宽 `proxy_read_timeout`；大文件建议先内网下载验证 |
| 面板打不开 | 8888 端口未放行（云安全组 + 系统防火墙） |
| 上传文件失败 | `client_max_body_size` 太小，或 URL 列表文件超过限制（默认已设 50m） |
| data 目录无法写入 | `chown -R www:www` 项目根目录；确认启动用户为 www |
| 任务抓取全部失败 | 目标站反爬（见 README「合规与已知限制」）：确认网络可达 alibaba.com；L3 需 `playwright install chromium` |

---

## 10. 安全与维护建议

- 面板：修改默认端口、开启二次验证；仅对可信 IP 开放 8888；
- 防火墙：**只放行 80/443**，后端 8000 仅本机回环（无需安全组放行）；
- 数据备份：定期备份 `/www/wwwroot/getimages/data/`（app.db + downloads 为全部业务数据）；
- 升级：拉取新代码 → 重新安装 requirements → `npm run build` 更新 dist → 重启 Python 项目；
- 抓取需登录的商品：SSH/宝塔终端到后端目录执行 `python -m app.engine.login`（首次需 `pip install playwright && playwright install --with-deps chromium`，`--with-deps` 补装 Linux 系统库），登录态自动存至 `data/cookie.txt` / `data/login_state.json`（本人账号、仅限合法用途）；
- 合规：仅抓取公开数据、遵守目标站 robots.txt 与服务条款；素材版权归原权利人，产出仅限学习与研究等合法用途（详见《交付文档.md》§3.10）。

---

## 附：宝塔面板安装速查

```bash
# 查看面板信息（地址/账号/密码）
bt default

# 常用：bt 命令交互菜单（改密码、改端口、重启面板等）
bt
```
