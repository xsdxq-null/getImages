# getImages · 本地部署教程（开发机 / 单机运行）

> 适用对象：在自己电脑（Windows / Linux / macOS）上本地运行本系统，用于开发联调或单机自用。
> 服务器/宝塔部署见《部署教程-宝塔.md》。本文对应《交付文档.md》§3.9 部署与运行。

---

## 0. 两种运行模式（先选一种）

| 模式 | 用途 | 特点 |
| --- | --- | --- |
| **开发模式** | 日常开发 / 联调 | 后端 8000 + Vite 前端 5173（热更新，`/api` 自动代理到后端） |
| **生产模式** | 本地正式使用 | 前端构建一次，由 FastAPI 静态托管，**单端口 8000** 访问 |

> 无论哪种模式，后端都是同一个服务；区别只在「前端如何提供」。

---

## 1. 环境要求

| 依赖 | 要求 | 说明 |
| --- | --- | --- |
| Python | **3.10+**（推荐 3.11–3.13） | 本项目后端实测 Python 3.13.15 |
| Node.js | **16+**（推荐 18/20/22） | 仅前端构建/开发需要 |
| SQLite | 无需安装 | Python 内置 |
| 端口 | 8000（后端）、5173（仅开发模式） | 冲突时见 §7 |

检查本机版本：

```bash
python --version      # 或 py -3.13 --version（Windows 多版本时）
node --version
npm --version
```

---

## 2. 获取代码

```bash
git clone <仓库地址> getimages
cd getimages
```

项目结构（运行时只依赖 `backend/` 与 `frontend/`）：

```text
getimages/
├─ backend/          # FastAPI 后端（app/ 包、requirements.txt、tests/）
├─ frontend/         # Vue3 前端（src/、package.json、vite.config.js）
├─ data/             # 运行时自动创建（SQLite + 下载文件 + 日志），勿手动提交
└─ 文档.md 等
```

---

## 3. 后端部署（两种模式共用）

### 3.1 创建虚拟环境

```bash
cd backend

# Windows (cmd / PowerShell)
python -m venv .venv
.venv\Scripts\activate

# Windows (Git Bash) / Linux / macOS
python -m venv .venv
source .venv/bin/activate
```

> 提示符出现 `(.venv)` 即激活成功。虚拟环境隔离依赖，避免污染系统 Python。

### 3.2 安装依赖

```bash
pip install -r requirements.txt
```

国内网络慢时，先切清华镜像源再安装：

```bash
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
pip install -r requirements.txt
```

> 可选：L3 反爬兜底（Playwright 浏览器），体积较大、非必需：
>
> ```bash
> pip install playwright && playwright install chromium
> ```

### 3.3 启动后端

```bash
# 必须在 backend 目录下执行（应用入口是 app.main 包）
uvicorn app.main:app --port 8000
```

首次启动自动完成：创建 `data/` 目录 → 初始化 `data/app.db`（tasks/products/resources 三表）→ 检测 `frontend/dist` 并托管。

看到以下日志即成功：

```text
INFO:     Uvicorn running on http://127.0.0.1:8000
```

> 常用参数：
>
> - `--port 8001`：换端口（8000 被占用时）；
> - `--host 0.0.0.0`：允许局域网内其他设备访问（注意防火墙）；
> - 生产模式多进程：`gunicorn app.main:app --workers 2 --worker-class uvicorn.workers.UvicornWorker --bind 127.0.0.1:8000`
>   （本项目调度器为单实例 asyncio + SQLite，**勿多开 worker 共享同一 data 目录**，单进程为安全默认）。

### 3.4 可选：一键登录（抓取需登录的商品）

阿里巴巴部分商品详情页要求登录（登录墙/地区限制），此时需登录后抓取：

```bash
# 一次性：安装浏览器（约 150MB，国内网络慢可配置镜像）
pip install playwright
playwright install chromium

# 日常使用：弹出浏览器窗口，像平时一样登录阿里巴巴，回车保存登录态
python -m app.engine.login
```

登录成功后在 `backend` 目录下自动生成（敏感信息，已被 `.gitignore` 忽略）：

- `data/cookie.txt` —— httpx/curl_cffi 快速层自动携带；
- `data/login_state.json` —— playwright 浏览器兜底层自动恢复。

> **用 Google 等第三方账号登录打不开新标签（一直转圈）？** 原因：playwright 弹出的浏览器**不走系统代理**，而 Google 登录需要代理。在运行登录命令的终端先设置代理环境变量再跑（端口按你的代理工具填，Clash 默认 `7890`）：
>
> ```powershell
> $env:PLAYWRIGHT_PROXY="http://127.0.0.1:7890"   # PowerShell
> # 或 CMD：set PLAYWRIGHT_PROXY=http://127.0.0.1:7890
> python -m app.engine.login
> ```
>
> 也可用 `setx PLAYWRIGHT_PROXY "http://127.0.0.1:7890"` 永久生效（新终端生效）。不想走代理的话，改用阿里巴巴的手机号验证码/邮箱等登录方式即可。

**然后重启后端**，抓取即可携带登录态。cookie 过期后重跑 `python -m app.engine.login` 刷新即可。

### 3.5 代理支持说明（可选）

需要走代理（如科学上网访问海外站点、Google 登录）时的配置逻辑：

**配置入口**：启动时读取环境变量，优先级从高到低：

```
PLAYWRIGHT_PROXY  →  ALL_PROXY  →  HTTPS_PROXY  →  HTTP_PROXY
```

**各抓取层的代理行为**：

| 层 | 走代理方式 | 说明 |
| --- | --- | --- |
| L1 httpx | **自动**读环境变量 | `httpx` 默认信任环境变量代理（`HTTP_PROXY`/`HTTPS_PROXY`） |
| L2 curl_cffi | **自动**读环境变量 | 基于 curl，同样读取 `http_proxy`/`https_proxy` |
| L3 playwright | **需显式配置** `PLAYWRIGHT_PROXY` | 浏览器进程不继承系统代理，只认配置项传入的代理 |
| 一键登录浏览器 | **需显式配置** `PLAYWRIGHT_PROXY` | 同上，登录弹窗浏览器也不走系统代理 |

**用法示例**（Clash 默认端口 7890，按你的代理工具实际端口填写）：

```powershell
# 临时（当前终端）
$env:PLAYWRIGHT_PROXY="http://127.0.0.1:7890"

# 永久（新终端生效）
setx PLAYWRIGHT_PROXY "http://127.0.0.1:7890"
```

> 要点：
> - **L1/L2 无需配置**：只要系统/终端设了 `HTTPS_PROXY` 等通用变量，httpx/curl_cffi 自动生效；
> - **L3 与登录浏览器必须配 `PLAYWRIGHT_PROXY`**：否则它们直连不走代理（这正是"Google 登录新标签打不开"的原因）；
> - **不配置不影响使用**：代理为空时全部直连，行为与不设代理完全一致。

> 合规声明：登录态仅用于你本人账号的合法用途；不破解验证码、不绕过付费墙；账号与合规风险由使用者自担。不登录也能抓取的公开商品无需此步骤。

---

## 4. 开发模式：启动前端（热更新）

另开一个终端：

```bash
cd frontend
npm install --registry=https://registry.npmmirror.com   # 国内镜像加速
npm run dev
```

访问 **<http://127.0.0.1:5173>**：

- 页面改动即时热更新；
- 请求 `/api/*` 由 Vite 自动代理到 `http://127.0.0.1:8000`（配置见 `frontend/vite.config.js`）；
- 开发时**两个终端都要保持运行**（后端 8000 + 前端 5173）。

---

## 5. 生产模式：构建前端（单端口使用）

```bash
cd frontend
npm install --registry=https://registry.npmmirror.com
npm run build        # 产物输出到 frontend/dist/
```

然后**重启后端**（让它重新检测并托管 `dist`）：

```bash
cd backend
uvicorn app.main:app --port 8000
```

直接访问 **<http://127.0.0.1:8000/>** 即可完整使用（页面 + API 同端口，无需 Vite）。

---

## 6. 验证清单

| 检查项 | 方式 | 预期 |
| --- | --- | --- |
| 后端健康 | 浏览器 `http://127.0.0.1:8000/api/health` | `{"status":"ok"}` |
| API 文档 | `http://127.0.0.1:8000/docs` | Swagger 交互文档 |
| 前端页面 | 访问 8000（生产）或 5173（开发） | 任务列表页正常 |
| 创建任务 | 上传 txt（每行一个商品 URL）→ 提交 | 任务运行，进度/日志实时滚动 |
| 打包下载 | 任务完成后点「打包下载」 | 下载 zip：`{task_id}/{product_id}/` 目录 + manifest.json |
| 数据落盘 | `ls data/` | `app.db`、`downloads/`、`logs/` |
| 测试 | `cd backend && python -m pytest tests -q` | **72 passed**（引擎 49 + API 23） |

---

## 7. 常见问题排查

| 现象 | 原因与处理 |
| --- | --- |
| `python` 打不开（Windows 弹出商店） | 系统把 `python` 映射到 Microsoft Store 别名。用 `py -3.13` 或完整路径（如 `C:\Users\<你>\AppData\Local\Programs\Python\Python313\python.exe`），或关闭「应用执行别名」 |
| `ModuleNotFoundError: No module named 'app'` | 启动目录不对：**必须在 `backend/` 目录下**执行 `uvicorn app.main:app` |
| 端口 8000 被占用 | `uvicorn app.main:app --port 8001`，前端代理仍指向 8000 的话同步改 `vite.config.js` |
| 端口 5173 被占用 | Vite 会自动使用 5174，按终端提示访问即可 |
| 前端白屏 / 404 | 生产模式未构建或未重启后端：确认 `frontend/dist/index.html` 存在并重启 uvicorn |
| 依赖安装报错 | 换清华源重试；个别包需较新 Python（本项目要求 3.10+） |
| 下载/上传中文乱码 | 终端编码：Windows 执行 `chcp 65001` 或用 Git Bash / PowerShell 7 |
| 抓取任务全部失败 | 目标站反爬：确认本机可访问 alibaba.com；必要时启用 L3（`playwright install chromium`）；详情见 README「合规与已知限制」 |
| 停止服务 | 后端终端按 `Ctrl+C`；开发模式前端终端同样 `Ctrl+C` |

---

## 8. 开机自启（可选）

- **Linux（systemd）**：参考《部署教程-宝塔.md》用 Python 项目管理器，或手写 systemd unit 运行 `uvicorn app.main:app`；
- **Windows**：`任务计划程序` 创建任务，启动 `backend\.venv\Scripts\uvicorn.exe app.main:app --port 8000`，工作目录设为 `backend`；
- **macOS**：`launchd` plist 或 `pm2`（需 `pm2 install pm2-windows-service` 等按平台适配）。

> 自用建议：生产模式构建一次后，开机启动只需拉起后端一个进程（8000 端口单端口访问）。

---

## 9. 升级 / 更新

```bash
git pull                              # 拉取新代码

# 先激活虚拟环境（前提：已按 §3.1 创建过 backend/.venv），再更新依赖
# Windows (cmd / PowerShell)
cd backend && .venv\Scripts\activate && pip install -r requirements.txt
# Windows (Git Bash) / Linux / macOS
cd backend && source .venv/bin/activate && pip install -r requirements.txt

cd ../frontend && npm run build       # 生产模式需重建前端
# 重启后端即可
```

> 升级不影响 `data/`（SQLite 与已下载文件保留）。重大版本请先备份 `data/`。

---

## 10. 合规提醒

仅抓取公开可见数据，不登录、不破解验证码、不绕过付费墙；遵守目标站 robots.txt 与服务条款；素材版权归原权利人，产出仅限学习与研究等合法用途（详见《交付文档.md》§3.10）。
