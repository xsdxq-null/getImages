# 阿里巴巴国际站商品图片/视频抓取系统 · PRD 需求文档

> 版本：v1.0 ｜ 状态：评审稿 ｜ 撰写日期：2026-08-12 ｜ 项目代号：getImages
> 关联文档：《交付文档.md》（技术方案）——本文档与之一致，视角互补：PRD 讲「做什么/为什么/验收什么」，交付文档讲「怎么做」。

---

## 1. 产品概述

### 1.1 背景与问题

电商运营与选品分析中，经常需要批量获取商品的图片与视频素材（主图、详情图、主图视频、详情视频），用于竞品研究、素材归档与内部参考。当前痛点：

- **人工逐个保存**：打开每个商品页，手工右键保存图片、下载视频，效率低、易遗漏；
- **素材零散无组织**：下载后无统一目录与命名，难以检索、归档与复用；
- **缺乏过程可见性**：批量下载时无法掌握进度与失败情况，中断后无法续跑；
- **反爬门槛**：目标站有风控机制，简单脚本易被拦截，需系统性应对。

### 1.2 产品目标

1. 输入一批商品详情页 URL，自动抓取每件商品的**主图、详情图、主图视频、详情视频**四类媒体资源，按统一规范落盘并生成清单；
2. 以 **Web 应用**形态交付：上传 URL 列表、创建/管理抓取任务、实时查看进度与日志、打包下载结果；
3. 以**全栈工程**标准建设：前后端分离、SQLite 持久化、异步任务调度、可量化验收；
4. 架构预留**多平台扩展**能力（速卖通 / 1688 / 京东等），本期仅实现阿里巴巴国际站。

### 1.3 产品范围

| 项目 | 范围 |
| --- | --- |
| 本期范围 | 阿里巴巴国际站商品详情页的四类媒体资源抓取 |
| 数据源 | 仅抓取**公开可见**的页面与媒体资源，不涉及登录态 |
| 输入 | 商品详情页 URL 列表（txt/csv） |
| 输出 | 按商品组织的媒体目录 + JSON 清单 + 打包下载（zip） |
| 交付形态 | 全栈 Web 应用（FastAPI 后端 + Vue 3 前端） |
| 本期不做（Won't） | 登录态抓取、破解验证码、绕过付费墙、自动下单/比价、关键词搜索入口（列入后续迭代） |

### 1.4 名词定义

| 术语 | 定义 |
| --- | --- |
| 主图 | 商品头图/相册图（gallery），详情页顶部轮播展示的图片集合 |
| 详情图 | 商品详情描述（description）HTML 中嵌入的图片 |
| 主图视频 | 商品主图区域的展示视频（多为 mp4，URL 内嵌于页面数据） |
| 详情视频 | 详情描述区域中的视频（`<video>` 或视频链接） |
| 商品 ID | 阿里国际站商品唯一标识（数字，详情页 URL 末段） |
| 任务 | 一次批量抓取动作的完整生命周期（含其下所有商品与资源） |
| 断点续传 | 任务中断/取消后可续跑，已下载资源跳过不重复下载 |

### 1.5 优先级定义（MoSCoW）

| 级别 | 含义 |
| --- | --- |
| Must（必须） | 本期发布必须具备，缺失则产品不可用 |
| Should（应该） | 重要但可临时绕过，有条件则纳入本期 |
| Could（可以） | 锦上添花，资源允许时实现 |
| Won't（不做） | 本期明确不做，列入后续迭代或永久排除 |

---

## 2. 用户与使用场景

### 2.1 目标用户角色

| 角色 | 画像 | 核心诉求 |
| --- | --- | --- |
| 电商运营专员 | 日常维护商品素材、做竞品陈列参考 | 批量、快速、不遗漏地获取素材，可打包归档 |
| 选品分析师 | 对比多商品图片/视频做选品研究 | 素材按商品组织、附带清单，便于检索对比 |
| 管理员（系统维护者） | 部署系统、配置参数、监控任务 | 限速/并发可配、任务可控可追踪、服务稳定 |

### 2.2 核心用户故事

| 编号 | 角色 | 用户故事 | 优先级 |
| --- | --- | --- | --- |
| US-01 | 运营 | 作为电商运营，我希望**上传一批商品链接创建抓取任务**，以便批量获取素材而不用逐个保存 | Must |
| US-02 | 运营 | 作为电商运营，我希望**每件商品的主图、详情图、主图视频、详情视频都能被自动抓取**，以便一次拿全四类素材 | Must |
| US-03 | 运营 | 作为电商运营，我希望**任务执行时实时看到进度与日志**，以便及时掌握完成情况与失败原因 | Must |
| US-04 | 运营 | 作为电商运营，我希望**任务完成后打包下载全部素材**，以便归档并分发给团队 | Must |
| US-05 | 分析 | 作为选品分析师，我希望**四类资源按商品分类存放并附 manifest 清单**，以便快速检索与对比 | Must |
| US-06 | 运营 | 作为电商运营，我希望**单件商品抓取失败不中断整个任务**，以便其余商品正常完成、失败项可重试 | Must |
| US-07 | 运营 | 作为电商运营，我希望**任务中断后可续跑且不重复下载已完成资源**，以便节省时间与流量 | Should |
| US-08 | 运营 | 作为电商运营，我希望**在商品详情里预览四类资源**，以便下载前确认内容 | Could |
| US-09 | 管理员 | 作为管理员，我希望**配置限速与并发参数**，以便合规抓取、避免对目标站造成压力 | Must |
| US-10 | 管理员 | 作为管理员，我希望**查看历史任务与结果**，以便回溯与统计 | Should |

### 2.3 使用场景

**主流程**（Happy Path）：

1. 用户打开「创建任务」页，拖拽/选择上传 URL 列表文件（txt/csv）；
2. 系统解析文件，展示商品数量与校验结果（非法链接剔除）；
3. 用户确认参数（限速、并发、是否跳过已下载）并提交，任务进入运行中；
4. 系统逐商品抓取四类资源，前端实时展示进度条与日志流；
5. 任务完成（或部分完成），用户点击打包下载获取 zip，内含按商品组织的目录与 manifest.json。

**异常流程**：

- 某商品页被风控拦截 → 系统自动降级抓取策略（httpx → TLS 模拟 → 浏览器），仍失败则标记该商品失败并继续后续商品；任务结束给出失败清单，支持重试；
- 任务被用户暂停/取消或服务重启 → 状态落库；再次启动时跳过已完成资源（断点续传）；
- 上传文件为空/格式错误/全为非法链接 → 前端即时提示，不允许创建任务；
- 目标站视频懒加载导致详情视频未提取 → 滚动触发后重新提取，仍缺失则记录到清单并标记。

---

## 3. 功能需求

### 3.1 功能总览

| 模块 | 编号 | 功能 | 优先级 |
| --- | --- | --- | --- |
| 数据抓取 | F1 | URL 列表上传与解析（去重、校验、商品 ID 提取） | Must |
| 数据抓取 | F2 | 四类资源提取（主图/详情图/主图视频/详情视频） | Must |
| 数据抓取 | F3 | 媒体下载与按规范落盘 | Must |
| 数据抓取 | F4 | manifest.json 清单生成 | Must |
| 数据抓取 | F5 | 失败处理与重试（单件失败不中断、指数退避、策略降级） | Must |
| 数据抓取 | F6 | 图片格式统一转 jpg（兼容 avif/webp 源） | Should |
| 任务管理 | F7 | 任务创建（上传 + 参数设置） | Must |
| 任务管理 | F8 | 任务控制（启动/暂停/取消/续跑） | Must |
| 任务管理 | F9 | 任务列表与状态展示 | Must |
| 任务管理 | F10 | 历史任务记录与回溯 | Should |
| 进度日志 | F11 | 任务级/商品级实时进度 | Must |
| 进度日志 | F12 | 实时日志流（按商品/级别过滤） | Must |
| 结果下载 | F13 | 全任务打包下载（zip） | Must |
| 结果下载 | F14 | 单商品单独下载 | Could |
| 扩展 | F15 | 平台适配层（速卖通/1688/京东） | Won't（迭代） |
| 扩展 | F16 | 关键词搜索入口 | Won't（迭代） |

### 3.2 数据抓取模块（F1–F6）

#### F1 URL 列表上传与解析

- 支持 txt（每行一个 URL）与 csv（含 `url` 列）；
- 提取商品 ID（URL 末段数字）并去重、剔除非法链接；展示有效商品数量；
- 文件为空/格式错误/全为非法链接时，前端提示且不允许创建任务。

#### F2 四类资源提取

- 主图：详情页数据 `product.mediaItems` 中图片项，URL 取高清优先、普通兜底；
- 主图视频：同数据源中视频项的 mp4 URL（内嵌页面数据，无需触发播放）；
- 详情图：详情描述 HTML 中所有 `<img>` 链接；
- 详情视频：详情描述 HTML 中的 `<video>`/mp4 链接（懒加载时滚动触发后重取）。

#### F3 媒体下载与落盘

- 按 `data/downloads/{task_id}/{product_id}/` 组织；
- 命名规则见 5.1；视频保留 mp4 原格式。

#### F4 manifest.json 清单

- 每件商品生成清单：商品 ID、标题、四类资源路径、抓取时间、状态；缺失资源如实记录。

#### F5 失败处理与重试

- 单件商品失败不中断任务；429/5xx 按指数退避重试（默认 3 次）；403 视为被拦截，自动切换抓取策略；
- 任务结束输出失败清单，失败商品支持单独重试。

#### F6 图片格式统一转 jpg

- avif/webp 源统一转 jpg；转换失败保留原格式并在清单中标记。

### 3.3 任务管理模块（F7–F10）

**F7 任务创建**：上传文件 + 参数（限速、并发、是否跳过已下载）；创建成功进入任务列表。
**F8 任务控制**：运行中可暂停/取消；已结束（含中断）任务可续跑；参数在创建后不可修改（可取消重建）。
**F9 任务列表**：展示名称、商品总数、完成/失败数、成功率、状态、创建时间；状态用标签区分。
**F10 历史记录**：任务与结果留存于 SQLite，列表可翻页回溯。

### 3.4 进度日志模块（F11–F12）

**F11 实时进度**：任务级（完成商品数/总数、成功率）+ 商品级（四类资源完成状态）；进度刷新延迟 ≤2s（SSE 推送）。
**F12 实时日志流**：按商品、按错误级别过滤；滚动查看，运行中持续追加。

### 3.5 结果下载模块（F13–F14）

**F13 全任务打包下载**：zip 内目录与命名规则一致；仅打包成功资源，失败项在清单中标注。
**F14 单商品下载**：从商品列表对单件商品打包下载。

---

## 4. 页面与交互流程

### 4.1 信息架构

```text
任务列表页 ──▶ 创建任务页（上传 + 参数）
   │
   ├──▶ 任务详情页（进度 / 商品列表 / 日志流）
   │        └──▶ 商品详情弹窗（四类资源预览）
   └──▶ 打包下载（zip）
```

### 4.2 页面流转

1. **任务列表页**（默认首页）：查看任务、点击「新建任务」进入创建页；
2. **创建任务页**：上传文件、设置参数、提交 → 返回任务列表，新任务进入运行中；
3. **任务详情页**：点击列表中的任务进入；顶部进度条，中部商品列表（可点开单商品预览），底部日志流；操作区提供暂停/取消/续跑/打包下载；
4. **商品详情弹窗**：Tabs 展示四类资源，图片可预览、视频可播放。

### 4.3 关键交互说明

- **上传**：拖拽 + 点击选择；上传后立即解析并展示有效商品数；
- **进度**：进度条 + 百分比 + 完成数/总数 + 成功率；失败商品在列表中红色标注并附原因；
- **日志**：自动滚动到底部，可暂停自动滚动、按级别过滤；
- **下载**：任务运行中不允许打包（可下载已完成的商品子集，可选）；任务结束/部分结束后可打包。

### 4.4 异常与边界交互

- 上传解析失败 → 内联错误提示，不阻断页面；
- 任务执行中刷新页面 → 状态从服务端恢复，进度不丢失；
- 打包过程中文件被占用/删除 → 提示重试；
- 商品数量过大（>500）→ 分批执行，进度与日志按批次推进，前端不卡顿（虚拟滚动）。

---

## 5. 业务规则

### 5.1 命名与目录规则

| 资源 | 命名 | 说明 |
| --- | --- | --- |
| 主图 | `main_001.jpg` | 序号从 001 递增 |
| 详情图 | `detail_001.jpg` | 同上 |
| 主图视频 | `main_video_01.mp4` | 序号从 01 递增 |
| 详情视频 | `detail_video_01.mp4` | 同上 |
| 清单 | `manifest.json` | 商品 ID/标题/四类资源路径/状态/抓取时间 |

目录：`data/downloads/{task_id}/{product_id}/`。

### 5.2 限速与合规规则

- 全局请求限速，默认 1–5 秒/请求（可配），并发默认 1–3；
- 尊重目标站 `Retry-After` 响应头；
- 仅抓取公开可见数据，遵守目标站 `robots.txt` 与服务条款。

### 5.3 断点续传规则

- 资源级幂等：文件已存在且大小匹配 → 跳过，不重复下载；
- 任务暂停/取消/服务重启后，续跑从断点继续。

### 5.4 失败处理与重试规则

- 单件失败不中断任务；429/5xx 按 `2^n` 指数退避重试（默认最多 3 次）；
- 403 不盲目重试，视为需要切换抓取策略（httpx → TLS 模拟 → 浏览器）；
- 重试耗尽标记失败，任务结束输出失败清单，支持单独重试。

### 5.5 格式转换规则

- avif/webp 源图片用 Pillow 转 jpg（avif 需 `pillow-avif-plugin`）；
- 转换失败保留原格式并在清单标记，不阻塞任务。

---

## 6. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 性能 | 单任务支持 500+ 商品分批抓取；进度/日志刷新延迟 ≤2s；打包下载在秒级内开始 |
| 可用性 | 任务中断可续跑；服务重启不丢任务与结果状态；单机部署即可运行 |
| 兼容性 | 现代浏览器（Chrome/Edge/Firefox 最近两个大版本）；Windows/Linux/macOS 可部署 |
| 安全与合规 | 仅公开数据；不收集非必要信息；素材版权归原权利人，仅限学习与研究等合法用途 |
| 可维护性 | 前后端分离、配置集中、日志分级、异常可追踪 |

---

## 7. 验收标准

### 7.1 验收清单

| 编号 | 验收项 | 量化标准 | 对应功能 |
| --- | --- | --- | --- |
| A1 | 四类资源提取正确率 | 抽样人工核对 ≥ 95% | F2 |
| A2 | 图片下载成功率 | ≥ 98%（排除目标站主动拦截） | F3 |
| A3 | 任务管理全流程 | 创建/启动/暂停/取消/续跑均可用 | F7–F9 |
| A4 | 进度与日志实时性 | SSE 推送，状态与日志可见即更新（≤2s） | F11–F12 |
| A5 | 打包下载 | zip 内容与目录命名规则一致 | F13 |
| A6 | 清单完整性 | manifest.json 字段齐全（ID/标题/四类资源/状态/时间） | F4 |
| A7 | 限速生效 | 实测请求间隔符合配置值 | F5/5.2 |
| A8 | 断点续传 | 中断后续跑不重复下载已完成资源 | 5.3 |

### 7.2 验收流程

1. 按 A1–A8 逐项执行验收用例（含 3–5 个真实商品 URL 冒烟）；
2. 所有 Must 优先级功能验收通过、A1–A8 达标 → 判定验收通过；
3. Should/Could 功能未全量交付不阻塞验收，但需在发布说明中标注。

---

## 8. 里程碑与发布计划

### 8.1 里程碑（与《交付文档.md》4.1 对齐）

| 里程碑 | 内容 | PRD 验收门禁 |
| --- | --- | --- |
| M1 抓取原型 | 脚本级验证单商品四类资源可提取、可下载 | F2/F3 在单商品上成立 |
| M2 后端 API + 任务系统 | FastAPI + SQLite + 调度器 + 三层递进抓取 | F1–F5/F7–F9 后端可用 |
| M3 前端界面 | Vue 3 + Element Plus：任务/进度/日志/下载 | F11–F13 页面可用 |
| M4 稳健化与联调 | 限速/重试/断点续传/格式转换/前后端联调 | A1–A8 预检 |
| M5 交付验收 | 全量验收 + 部署说明 + 文档定稿 | A1–A8 全部通过 |

### 8.2 发布计划

- **内测（M4 末）**：内部 3–5 人试用，收集使用反馈与异常日志；
- **试运行（M5）**：小批量真实任务验证稳定与合规；
- **正式发布（M5 验收通过后）**：交付部署说明与验收记录。

---

## 9. 风险、合规与迭代规划

### 9.1 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 目标站反爬策略升级 | 抓取失败率上升 | 三层递进降级 + 可选代理 + 升级 stealth；最坏转商业 API |
| 页面结构改版 | 提取失效 | 提取器集中封装，字段变化快速修复 |
| 视频懒加载/防盗链 | 详情视频缺失 | 滚动触发重新提取；下载带页面 Referer |
| Python 环境不兼容新依赖 | 后端无法启动 | 环境要求明确 Python 3.10+（见交付文档 3.9） |
| 前端联调返工 | 周期延长 | 接口契约先行（OpenAPI 生成前端类型） |

### 9.2 合规边界

- 仅抓取公开可见数据，不登录、不破解验证码、不绕过付费/登录墙；
- 遵守目标站 `robots.txt` 与服务条款，限速访问；
- 素材版权归原权利人，本系统产出仅限学习与研究等合法用途；商业使用需获得授权或改用官方 API；
- 对个人/企业数据抓取的法律边界（如目标地域数据保护法规）由使用者自行评估并承担相应责任。

### 9.3 后续迭代候选

| 版本 | 候选功能 |
| --- | --- |
| v1.1 | 关键词搜索入口（F16）、xlsx 导出、单商品下载完善（F14） |
| v2.0 | 多平台扩展（速卖通/1688/京东，F15）、官方 API 接入、分布式任务（Celery + Redis）、登录态支持（按需，注意合规） |

---

## 10. 附录

### 10.1 与《交付文档.md》对应关系

| PRD 章节 | 交付文档章节 |
| --- | --- |
| 1 产品概述（范围/名词） | 1 项目概述 |
| 3 功能需求（抓取/全栈） | 2 需求规格、3 技术方案 |
| 5 业务规则 | 2.2 非功能需求、3.4 命名规范、3.7 抓取引擎 |
| 7 验收标准 | 4.3 验收标准（A1–A8） |
| 8 里程碑 | 4.1 里程碑（M1–M5） |
| 9 风险与迭代 | 4.4 风险应对、4.5 扩展路线 |

### 10.2 参考

- 《交付文档.md》v1.0（2026-08-12）
- 调研参考见《交付文档.md》第 5 章附录

---
